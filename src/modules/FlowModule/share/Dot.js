export class Dot {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }

  /**
   * @param {number} x
   * @param {number} y
   * @param {CanvasRenderingContext2D} ctx
   */
  animate(x, y, ctx) {}
}
